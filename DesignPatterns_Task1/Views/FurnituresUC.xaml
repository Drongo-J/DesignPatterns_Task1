<UserControl x:Class="DesignPatterns_Task1.Views.FurnituresUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesignPatterns_Task1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Wheat">
        <Canvas>
            <Button Canvas.Left="10" Canvas.Top="20" Height="30" Width="60" Command="{Binding BackCommand}" HorizontalAlignment="Center" VerticalAlignment="Top">Back</Button>
            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto" HorizontalAlignment="Left" Canvas.Left="90" VerticalAlignment="Center">
                <WrapPanel x:Name="Furnitures" Height="Auto" Width="620" HorizontalAlignment="Center"  VerticalAlignment="Top" Canvas.Left="90"/>
            </ScrollViewer>
        </Canvas>
    </Grid>
</UserControl>
